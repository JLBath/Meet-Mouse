<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="/css/style.css">
    <title>Meet Mouse</title>
</head>
<body>
    <nav>
        <h2><a href="/"><img src="/images/mooseGreen.png" class="logo" alt="A green mouse, with the words meet mouse written with its tail" ></a></h2>
        <a href='/'>Home</a>
        <a href='/events'>Connections</a>
        <a href='/events/new'>Create a New Connection</a>
        
        <%if(!user) {%>
            <a href='/users/new'>Sign Up</a>
            <a href='/users/login'>Login</a>
        <%} else {%>
            <a href='/users/profile'>Profile</a>
            <a href='/users/logout'>Logout</a>
        <%}%>
       
        
    </nav>
    </nav>

    <% if(errorMessages.length > 0) {%>
        <% errorMessages.forEach(message=>{%>
            <div class='error'>
                <%=message%>
            </div>
            <%});%>
                <%}%>
    
                    <% if(successMessages.length > 0) {%>
    
                        <% successMessages.forEach(message=>{%>
                            <div class='success'>
                                <%=message%>
                            </div>
                            <%});%>
                                <%}%>